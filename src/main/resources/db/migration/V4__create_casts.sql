CREATE CAST (varchar AS analysis_usage_enum) WITH INOUT AS IMPLICIT;

CREATE CAST (varchar AS analysis_status_enum) WITH INOUT AS IMPLICIT;

CREATE CAST (varchar AS appointment_status_enum) WITH INOUT AS IMPLICIT;

CREATE CAST (varchar AS day_of_week_enum) WITH INOUT AS IMPLICIT;

CREATE CAST (varchar AS gender_enum) WITH INOUT AS IMPLICIT;

CREATE CAST (varchar AS notification_enum) WITH INOUT AS IMPLICIT;

CREATE CAST (varchar AS pay_receipt_status_enum) WITH INOUT AS IMPLICIT;

CREATE CAST (varchar AS payment_method_enum) WITH INOUT AS IMPLICIT;

CREATE CAST (varchar AS payment_type_enum) WITH INOUT AS IMPLICIT;

CREATE CAST (varchar AS role_enum) WITH INOUT AS IMPLICIT;